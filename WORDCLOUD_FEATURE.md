# 🌈 词云功能说明

## 功能概述

系统现在支持自动生成和展示关键词词云，可视化展示每日热门关键词和话题趋势。

---

## 🎯 功能特性

### 1. 自动生成词云

**触发时机**:
- 每天生成日报时自动生成词云
- 手动触发生成

**生成内容**:
- 总体词云（所有文章）
- 各层面词云（政治、经济、技术、金融科技）
- 各关键词类型词云（公司、行业、经济指标、市场动态）

### 2. 词云展示页面

**访问路径**: `/wordcloud.html`

**功能**:
- 按日期查看词云
- 按层面筛选
- 按关键词类型筛选
- 点击图片放大查看
- 手动生成词云

### 3. 词云API

**端点**:
- `GET /wordcloud/image` - 获取词云图片
- `GET /wordcloud/list` - 列出可用词云
- `GET /wordcloud/generate` - 生成词云
- `POST /wordcloud/cleanup` - 清理旧词云

---

## 🎨 词云样式

### 配置参数

- **尺寸**: 1200x600 像素
- **最大词数**: 100个
- **背景色**: 白色
- **字体**: 自动检测系统中文字体
- **配色方案**: viridis（蓝绿渐变）
- **词频权重**: 出现次数越多，字体越大

### 支持的字体

系统会自动检测以下字体：
- Windows: 黑体、微软雅黑、宋体
- Linux: WenQuanYi Micro Hei
- macOS: PingFang、Arial Unicode

---

## 📊 使用方法

### 方法1: 自动生成（推荐）

词云会在每天生成日报时自动生成，无需手动操作。

### 方法2: 手动生成

1. 访问 `/wordcloud.html`
2. 选择日期
3. （可选）选择层面或关键词类型
4. 点击"生成词云"按钮

### 方法3: API调用

```bash
# 生成总体词云
curl "http://localhost:5000/wordcloud/generate?date=2025-10-15"

# 生成特定层面词云
curl "http://localhost:5000/wordcloud/generate?date=2025-10-15&category=技术"

# 生成特定类型词云
curl "http://localhost:5000/wordcloud/generate?date=2025-10-15&keyword_type=公司"

# 获取词云图片
curl "http://localhost:5000/wordcloud/image?date=2025-10-15" -o wordcloud.png

# 列出所有词云
curl "http://localhost:5000/wordcloud/list?date=2025-10-15"
```

---

## 🗂️ 文件存储

### 存储位置

词云图片存储在: `static/wordclouds/`

### 文件命名规则

- 总体词云: `YYYYMMDD.png`
- 层面词云: `YYYYMMDD_层面.png`
- 类型词云: `YYYYMMDD_类型.png`

示例:
- `20251015.png` - 2025年10月15日总体词云
- `20251015_技术.png` - 技术层面词云
- `20251015_公司.png` - 公司类型词云

### 自动清理

系统会自动清理30天前的词云图片，节省存储空间。

---

## 🔧 技术实现

### 后端服务

**WordCloudService** (`app/services/wordcloud_service.py`)

主要方法:
- `generate_wordcloud_from_keywords()` - 从关键词生成词云
- `generate_daily_wordclouds()` - 生成每日所有词云
- `get_wordcloud_path()` - 获取词云路径
- `cleanup_old_wordclouds()` - 清理旧词云

### 数据来源

词云基于文章的结构化关键词生成：
1. 从数据库获取指定日期的所有文章
2. 提取文章的关键词（JSON格式）
3. 统计关键词出现频率
4. 根据频率生成词云

### 依赖库

- `wordcloud` - 词云生成
- `jieba` - 中文分词
- `pillow` - 图像处理

---

## 📱 界面说明

### 筛选栏

```
[日期] [层面] [关键词类型] [生成词云]
```

### 词云网格

词云以网格形式展示，每个词云卡片包含：
- 标题（总体/层面/类型）
- 标签（层面或类型）
- 词云图片
- 日期信息

### 图片查看

- 点击词云图片可放大查看
- 按ESC键或点击背景关闭

---

## 💡 使用场景

### 1. 快速了解热点

查看总体词云，一眼看出当天的热门话题和关键词。

### 2. 领域分析

查看特定层面的词云，了解该领域的热点：
- 政治层面：政策、监管动态
- 经济层面：市场、企业动态
- 技术层面：技术趋势、创新
- 金融科技：金融创新、支付

### 3. 主题追踪

查看特定类型的词云，追踪特定主题：
- 公司：关注哪些企业
- 行业：哪些行业受关注
- 经济指标：关键数据指标
- 市场动态：市场变化趋势

### 4. 趋势对比

对比不同日期的词云，发现话题变化趋势。

---

## 🎯 最佳实践

### 1. 定期查看

建议每天查看词云，了解当天的热点话题。

### 2. 结合文章阅读

从词云发现感兴趣的关键词后，在文章列表中搜索该关键词，深入了解相关内容。

### 3. 多维度分析

- 先看总体词云，了解整体情况
- 再看各层面词云，了解细分领域
- 最后看类型词云，关注特定维度

### 4. 导出和分享

词云图片可以直接下载，用于报告、演示等场景。

---

## 🔄 自动化流程

### 每日报告生成流程

```
1. 爬取文章
2. 生成AI摘要和关键词
3. 生成日报
4. 自动生成词云 ✨
   - 总体词云
   - 4个层面词云
   - 4个类型词云
5. 发送邮件报告
```

### 词云生成流程

```
1. 从数据库获取文章和关键词
2. 统计关键词频率
3. 应用筛选条件（层面/类型）
4. 生成词云图片
5. 保存到文件系统
6. 返回图片路径
```

---

## 📊 数据统计

### 词云包含的信息

- **词频**: 关键词出现次数
- **权重**: 字体大小反映重要程度
- **分布**: 词的位置和排列

### 统计维度

- 按日期统计
- 按层面统计
- 按关键词类型统计

---

## 🚀 未来增强

### 计划中的功能

1. **词云动画**
   - 展示词云的生成过程
   - 词的飞入动画效果

2. **交互式词云**
   - 点击词云中的词跳转到相关文章
   - 鼠标悬停显示词频

3. **词云对比**
   - 并排对比不同日期的词云
   - 高亮显示变化的词

4. **自定义样式**
   - 选择不同的配色方案
   - 调整词云形状（圆形、心形等）

5. **词云报告**
   - 生成词云分析报告
   - 导出词频统计数据

6. **实时词云**
   - 实时更新的词云
   - 显示最新的热门话题

---

## 🔧 配置说明

### 词云配置

在 `app/services/wordcloud_service.py` 中可以调整：

```python
self.wordcloud_config = {
    'width': 1200,              # 宽度
    'height': 600,              # 高度
    'background_color': 'white', # 背景色
    'max_words': 100,           # 最大词数
    'relative_scaling': 0.5,    # 相对缩放
    'min_font_size': 10,        # 最小字体
    'max_font_size': 100,       # 最大字体
    'colormap': 'viridis',      # 配色方案
    'prefer_horizontal': 0.7,   # 水平词比例
}
```

### 清理配置

默认保留30天的词云，可以调整：

```python
wordcloud_service.cleanup_old_wordclouds(days=30)
```

---

## 📝 注意事项

1. **首次使用需要安装依赖**:
   ```bash
   pip install wordcloud pillow jieba
   ```

2. **中文字体**:
   - 系统会自动检测中文字体
   - 如果词云中文显示异常，请安装中文字体

3. **性能考虑**:
   - 词云生成需要一定时间（通常1-3秒）
   - 建议在后台自动生成，而不是实时生成

4. **存储空间**:
   - 每个词云约100-500KB
   - 每天生成约9个词云
   - 30天约占用100-200MB空间

---

## 🎉 功能已完成

词云功能已完全实现并集成到系统中：

- ✅ 自动生成词云
- ✅ 词云展示页面
- ✅ API接口
- ✅ 自动清理
- ✅ 多维度筛选

**立即体验**: 访问 `/wordcloud.html` 查看词云！
