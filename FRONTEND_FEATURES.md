# 前端新功能说明

## 新增功能

### 1. 📧 发送报告到指定邮箱

#### 功能位置
- **首页报告卡片**：每个报告卡片上有"📧 发送"按钮
- **报告详情页面**：右上角有"📧 发送报告"按钮

#### 使用方法

**方式一：从首页快速发送**
1. 在首页找到要发送的报告
2. 点击报告卡片上的"📧 发送"按钮
3. 自动跳转到报告详情页面

**方式二：从报告详情页面发送**
1. 进入报告详情页面
2. 点击右上角"📧 发送报告"按钮
3. 在弹出的对话框中选择收件人：
   - **选择已有收件人**：勾选邮件管理中已添加的邮箱
   - **输入新邮箱**：在输入框中输入邮箱地址（多个用逗号分隔）
   - **附加PDF**：可选择是否附加PDF文件
4. 点击"发送"按钮

#### 功能特点
- ✅ 支持选择多个收件人
- ✅ 支持临时输入新邮箱地址
- ✅ 支持附加PDF文件（可选）
- ✅ 自动验证邮箱格式
- ✅ 发送前确认提示
- ✅ 实时反馈发送结果

#### API接口
```
POST /api/settings/send-email
```

**请求参数：**
```json
{
  "report_date": "2025-10-11",
  "recipients": ["user1@example.com", "user2@example.com"],
  "attach_pdf": false
}
```

**响应：**
```json
{
  "success": true,
  "message": "报告已成功发送到 2 个邮箱"
}
```

---

### 2. ⏰ 定时时间设置

#### 功能位置
- **系统设置页面**：导航栏 → 系统设置

#### 使用方法
1. 点击导航栏的"系统设置"
2. 在"定时任务设置"区域：
   - 查看当前定时时间
   - 选择新的定时时间（默认08:00）
   - 点击"保存定时设置"按钮
3. 确认修改
4. **重启服务**使设置生效

#### 功能特点
- ✅ 可视化时间选择器
- ✅ 显示当前定时时间
- ✅ 自动保存到配置文件
- ✅ 修改前确认提示
- ✅ 提醒需要重启服务

#### 默认设置
- 默认定时时间：**08:00**（每天早上8点）
- 时区：Asia/Shanghai

#### API接口

**获取当前定时时间：**
```
GET /api/settings/schedule-time
```

**响应：**
```json
{
  "schedule_time": "08:00",
  "timezone": "Asia/Shanghai"
}
```

**更新定时时间：**
```
POST /api/settings/schedule-time
```

**请求参数：**
```json
{
  "schedule_time": "09:00"
}
```

**响应：**
```json
{
  "success": true,
  "message": "定时时间已更新为 09:00",
  "note": "需要重启服务才能生效"
}
```

---

## 页面更新

### 更新的页面

1. **index.html（首页）**
   - 添加"📧 发送"按钮到报告卡片
   - 添加"系统设置"导航链接

2. **report-detail.html（报告详情）**
   - 添加"📧 发送报告"按钮
   - 添加发送邮件模态框
   - 添加收件人选择功能
   - 添加"系统设置"导航链接

3. **settings.html（系统设置）** - 新建
   - 定时任务设置界面
   - 时间选择器
   - 当前设置显示

### 新增的API

1. **app/api/settings.py** - 新建
   - `POST /api/settings/send-email` - 发送报告到指定邮箱
   - `GET /api/settings/schedule-time` - 获取定时时间
   - `POST /api/settings/schedule-time` - 更新定时时间

---

## 使用场景

### 场景1：临时发送报告给客户
1. 客户要求查看某天的报告
2. 在首页找到对应日期的报告
3. 点击"📧 发送"按钮
4. 输入客户邮箱地址
5. 选择是否附加PDF
6. 发送

### 场景2：定期发送给固定收件人
1. 在"邮件管理"中添加固定收件人
2. 在报告详情页面点击"📧 发送报告"
3. 勾选需要发送的收件人
4. 发送

### 场景3：调整报告生成时间
1. 进入"系统设置"
2. 修改定时时间（如从08:00改为09:00）
3. 保存设置
4. 重启服务
5. 系统将在新时间自动生成报告

---

## 技术实现

### 前端技术
- 原生JavaScript
- Fetch API
- 模态框组件
- 表单验证

### 后端技术
- FastAPI路由
- Pydantic数据验证
- YAML配置文件读写
- 邮件服务集成

### 数据流程

**发送邮件流程：**
```
用户点击发送 
  → 选择收件人 
  → 前端验证 
  → API请求 
  → 后端验证 
  → 读取报告 
  → 发送邮件 
  → 返回结果
```

**更新定时时间流程：**
```
用户选择时间 
  → 前端验证格式 
  → API请求 
  → 后端验证 
  → 更新config.yaml 
  → 重新加载配置 
  → 返回结果
```

---

## 注意事项

### 发送邮件功能
1. ⚠️ 确保SMTP配置正确
2. ⚠️ 检查邮箱授权码是否有效
3. ⚠️ 大量发送可能触发邮件服务商限制
4. ⚠️ 建议先发送测试邮件验证配置

### 定时时间设置
1. ⚠️ 修改后必须重启服务才能生效
2. ⚠️ 时间格式必须为 HH:MM（24小时制）
3. ⚠️ 建议避开系统维护时间
4. ⚠️ 确保服务器时区设置正确

---

## 未来优化方向

### 短期优化
- [ ] 添加发送历史记录
- [ ] 支持批量发送多个报告
- [ ] 添加邮件模板自定义
- [ ] 支持定时任务的启用/禁用

### 长期优化
- [ ] 支持多个定时任务
- [ ] 添加发送统计和分析
- [ ] 支持邮件发送队列
- [ ] 添加邮件发送失败重试机制
- [ ] 支持Webhook通知

---

## 相关文件

### 前端文件
- `templates/index.html` - 首页（添加发送按钮）
- `templates/report-detail.html` - 报告详情（添加发送功能）
- `templates/settings.html` - 系统设置页面（新建）
- `static/css/style.css` - 样式文件（已有模态框样式）

### 后端文件
- `app/api/settings.py` - 设置API（新建）
- `app/main.py` - 主应用（注册新路由）
- `app/services/email_service.py` - 邮件服务（已有）
- `app/config.py` - 配置管理（已有）

### 配置文件
- `config.yaml` - 主配置文件
- `.env` - 环境变量配置

---

## 测试建议

### 发送邮件功能测试
1. 测试选择单个收件人
2. 测试选择多个收件人
3. 测试输入新邮箱地址
4. 测试邮箱格式验证
5. 测试附加PDF功能
6. 测试发送失败处理

### 定时时间设置测试
1. 测试获取当前时间
2. 测试更新时间
3. 测试时间格式验证
4. 测试配置文件更新
5. 测试重启后生效

---

## 快速开始

1. **启动服务**
```bash
python start_server.py
```

2. **访问系统设置**
```
http://localhost:8000/settings.html
```

3. **发送测试报告**
   - 访问首页
   - 找到任意报告
   - 点击"📧 发送"
   - 输入测试邮箱
   - 发送

4. **修改定时时间**
   - 访问系统设置
   - 选择新时间
   - 保存
   - 重启服务

---

## 常见问题

**Q: 为什么修改定时时间后没有生效？**
A: 需要重启服务才能生效。修改配置后，请重启 `start_server.py`。

**Q: 发送邮件失败怎么办？**
A: 检查SMTP配置、网络连接、邮箱授权码是否正确。可以先在"邮件管理"中发送测试邮件。

**Q: 可以同时发送给多少个收件人？**
A: 理论上没有限制，但建议单次不超过50个，避免触发邮件服务商的限制。

**Q: PDF附件在哪里？**
A: 如果报告生成时启用了PDF功能，PDF文件会保存在 `data/reports/` 目录下。

**Q: 如何查看发送历史？**
A: 当前版本暂不支持，可以查看日志文件 `logs/app.log` 中的发送记录。
